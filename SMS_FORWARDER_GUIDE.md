# دليل تشغيل وضبط نظام الشحن التلقائي عبر رسائل الـ SMS (فودافون كاش / إنستاباي)

يوضح هذا الدليل كيفية عمل نظام الشحن الآلي وكيفية ربط الهاتف الذي يحتوي على شريحة المحفظة (فودافون كاش / اتصالات كاش / أورانج كاش / إنستاباي) بسيرفر البوت لكي يتم شحن الرصيد تلقائياً للعملاء بمجرد وصول رسالة التحويل.

---

## 1. فكرة وطريقة عمل النظام

1. **العميل يطلب الشحن في البوت**:
   - يضغط على "💳 شحن الرصيد" ثم يختار "⚡ شحن فوري تلقائي".
   - يكتب المبلغ المطلوب (مثلاً: `50` أو `100`).
   - يعطيه البوت رقم المحفظة المحدد في ملف `.env` (`AUTO_TOPUP_WALLET_RECEIVER`) مع المبلغ المطلوب بالضبط.
2. **التحويل وإدخال رقم المرسل**:
   - العميل يقوم بالتحويل من محفظته إلى رقمك.
   - يضغط العميل على زر `[✅ تم التحويل - تأكيد بالرقم]` ويكتب رقم الهاتف الذي حوّل منه (مثلاً: `01012345678`).
3. **التحقق الآلي وشحن الرصيد**:
   - التطبيق الموجود على هاتفك يقرأ رسالة الـ SMS الواردة من الشركة ويرسلها إلى سيرفر البوت عبر الويب هوك (`/api/sms/webhook`).
   - يقوم البوت بتحليل نص الرسالة، استخراج المبلغ ورقم هاتف الراسل ورقم العملية (Transaction ID).
   - يتم مطابقة التحويل مع طلب العميل وإضافة الرصيد إلى محفظته فوراً وإرسال إشعار تليجرام له برصيده الجديد وكود العملية.

---

## 2. معايير الأمان والحماية (مراجعة الأمان)

النظام مصمم بحماية مشددة لمنع أي تلاعب أو احتيال:

- 🛡️ **منع استخدام التحويل أكثر من مرة (Single-Use Guarantee)**:
  - كل عملية لها رقم فريد (`trx_id UNIQUE`) مسجل في جدول `sms_transfers`.
  - بمجرد شحن العملية يتم تسجيلها كـ `claimed` برقم حساب التليجرام الخاص بالمستخدم وتاريخ الاستخدام.
  - لا يمكن لأي مستخدم آخر أو لنفس المستخدم إعادة استخدام نفس التحويل إطلاقاً.
- 🛡️ **منع التكرار على مستوى دفتر العمليات (Idempotent Ledger)**:
  - جدول `ledger` يحتوي على قيد فريد `UNIQUE(idempotency_key)`.
  - كل شحن يسجل بمفتاح فريد `sms_topup:vf_XXXXXX`، مما يجعل تكرار إضافة الرصيد مستحيلاً تقنياً على مستوى محرك قاعدة البيانات SQLite.
- 🛡️ **منع هجمات التخمين (Anti-Brute-Force)**:
  - حد أقصى 5 محاولات لكل طلب شحن لتخمين رقم الراسل. بعد 5 محاولات خاطئة يتم إيقاف الطلب تلقائياً لحماية النظام.
- 🛡️ **نافذة صلاحية زمنية (Time-Window Expiry)**:
  - لا يقبل النظام تحويلات قديمة تم استلامها قبل أكثر من 30 دقيقة (`AUTO_TOPUP_EXPIRY_MINUTES`) لمنع استغلال أي تحويلات منسية.
- 🛡️ **حماية رابط الويب هوك بكلمة سر (Webhook Secret)**:
  - يمكنك تعيين كلمة سر سرية في `SMS_WEBHOOK_SECRET`؛ سيرفض السيرفر أي اتصال خارجي لا يحمل هذه الكلمة برمز 401 Unauthorized.

---

## 3. إعداد تطبيق تحويل الرسائل على هاتف الأندرويد

لتحويل رسائل الـ SMS تلقائياً من هاتفك إلى البوت، يتم تثبيت تطبيق تحويل SMS على الهاتف الذي يحتوي على شريحة فودافون كاش:

### التطبيقات المقترحة (المجانية والموثوقة):
1. **SMS Forwarder** (تطبيق مفتوح المصدر ومتاح على Google Play أو GitHub - من تطوير lanrensms أو canlio).
2. أو **MacroDroid** (تطبيق أتمتة شهير وسهل جداً).

### خطوات الضبط على تطبيق SMS Forwarder:
1. قم بتحميل وتثبيت التطبيق على هاتف المحفظة.
2. امنح التطبيق صلاحيات قراءة الرسائل (SMS Permission) والعمل في الخلفية دون توفير طاقة البطارية (Disable Battery Optimization).
3. افتح قائمة **Senders / مرسلو الرسائل** وأضف:
   - فودافون كاش: المرسل هو `VodafoneCash` أو `VF-Cash`.
   - إنستاباي: `InstaPay` أو البنك الخاص بك.
   - أو اتركه لجميع الرسائل مع فلتر كلمة: `تم استلام مبلغ`.
4. افتح قائمة **Forward Rules / قواعد الإرسال** واختر إضافة قاعدة جديدة:
   - نوع الهدف: **Webhook / HTTP Request**.
   - الرابط (URL):
     ```text
     http://YOUR_SERVER_IP:3000/api/sms/webhook
     ```
     *(استبدل `YOUR_SERVER_IP` بعنوان IP السيرفر الخاص بك أو رابط الدومين/Railway)*
   - طريقة الطلب: **POST**.
   - تنسيق البيانات: **JSON**.
   - في خانة Headers أضف (في حال قمت بتعيين كلمة سر في `.env`):
     ```text
     x-webhook-secret: your_secret_webhook_token
     ```
   - نص الـ JSON في الـ Body:
     ```json
     {
       "message": "[msg]"
     }
     ```
     *(رمز `[msg]` أو `[body]` في التطبيق يقوم بنقل نص الرسالة المستلمة تلقائياً)*
5. احفظ الإعدادات وقم بعمل اختبار (Test) للتأكد من نجاح الاتصال بالسيرفر.

---

## 4. تجربة ومحاكاة النظام محلياً (بدون تحويل أموال حقيقية)

تم توفير أداة محاكاة مخصصة لاختبار النظام محلياً: `bin/simulate-sms.js`.

### خطوات التجربة:
1. شغّل البوت كالمعتاد:
   ```powershell
   node bin/m-automation-bot.js
   ```
2. ادخل على البوت في تليجرام واضغط **💳 شحن الرصيد** -> **⚡ شحن فوري تلقائي**.
3. أدخل مبلغ الشحن مثلاً: `50`.
4. سيعطيك البوت رقم المحفظة. اضغط **✅ تم التحويل - تأكيد بالرقم**.
5. سيطلب منك البوت رقم الهاتف المحول منه.
6. افتح نافذة تيرمينال جديدة ونفذ أمر إرسال رسالة وهمية مطابقة:
   ```powershell
   node bin/simulate-sms.js --amount 50 --sender 01012345678
   ```
7. الآن ادخل على البوت واكتب الرقم `01012345678`.
8. ستجد أن البوت أضاف الرصيد فوراً وأرسل لك رسالة تأكيد مع كود العملية والرصيد الجديد!
9. جرب إدخال نفس الرقم مجدداً أو من حساب آخر لتتأكد من أن النظام يرفض العملية فوراً ولا يضيف الرصيد إلا مرة واحدة فقط.
