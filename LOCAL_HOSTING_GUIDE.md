# 🖥️ دليل تشغيل جهازك كخادم محلي (Local VPS Host) للبوت

تم إعداد ملفات التشغيل المخصصة لويندوز لتشغيل بوت التليجرام على جهازك في الخلفية 24/7 تماماً مثل الـ VPS لحين توفير استضافة سحابية جديدة.

---

## 🚀 خطوات التشغيل لأول مرة:

### 1️⃣ إعداد ملف التكوين (`.env`)
- قم بتشغيل الملف: **`setup_env.bat`**
- سيقوم بإنشاء ملف `.env` وتوليد مفتاح تشفير آمن 64 خانة تلقائياً.
- افتح ملف `.env` وضع فيه:
  - `M_AUTOMATION_BOT_TOKEN`: توكن البوت من BotFather.
  - `M_AUTOMATION_SUPER_ADMIN_IDS`: الآيدي الخاص بحسابك على تليجرام.

---

### 2️⃣ تشغيل البوت في الخلفية (VPS Mode)
- **للتشغيل الصامت في الخلفية (بدون أي شاشات سوداء):**
  - انقر مرتين على: **`start_bot_background.vbs`**
  - سيعمل البوت والمراقب (Watchdog) في الخلفية تلقائياً، وفي حال انقطاع النت أو حدوث خطأ سيعيد تشغيل نفسه تلقائياً كل 5 ثوانٍ.

- **للتشغيل مع نافذة لمشاهدة العمليات مباشرة (Live Mode):**
  - انقر مرتين على: **`start_bot_visible.bat`**

---

### 3️⃣ التشغيل التلقائي مع فتح الويندوز (Auto-Start on Boot)
- انقر مرتين على: **`enable_autostart_on_boot.bat`**
- سيتم إضافة البوت لقائمة الإقلاع، وبالتالي سيعمل جهازك كخادم VPS: كلما فتحت الكمبيوتر سيبدأ البوت في الخلفية فوراً بدون تدخل منك!
- لإلغاء هذه الميزة لاحقاً: انقر على **`disable_autostart.bat`**.

---

### 4️⃣ أدوات المراقبة والتحكم:
- **لفحص حالة البوت وقراءة السجلات:** انقر على **`status_and_logs.bat`**
- **لإيقاف البوت تماماً:** انقر على **`stop_bot.bat`**
- **ملف السجلات المحفوظ:** تجده في `runtime/bot.log`.
